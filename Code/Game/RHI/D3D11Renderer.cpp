#include "Game/RHI/D3D11Renderer.hpp"
#include "Game/RHI/RHIDeviceWindow.hpp"
#include <d3d11_1.h>

//---------------------------------------------------------------------------------------------------------------------------
void D3D11Renderer::SetViewport() {

/*
	// Setup the viewport
	D3D11_VIEWPORT vp;
	vp.Width = (FLOAT)width;
	vp.Height = (FLOAT)height;
	vp.MinDepth = 0.0f;
	vp.MaxDepth = 1.0f;
	vp.TopLeftX = 0;
	vp.TopLeftY = 0;
	RHIDeviceWindow::Get()->GetD3DContext()->RSSetViewports(1, &vp);*/
}